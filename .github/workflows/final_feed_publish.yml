name: Daily Curation (Stage 2)

on:
  schedule:
    - cron: '0 2 * * *'  # 08:00 AM Bangladesh time (18:00 UTC)
  workflow_dispatch:

jobs:
  curate-final:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # Cache pip packages (general)
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}-v1
          restore-keys: |
            ${{ runner.os }}-pip-

      # Cache installed site-packages (libraries)
      - name: Cache Python site-packages
        uses: actions/cache@v4
        with:
          path: |
            /opt/hostedtoolcache/Python/3.11*/x64/lib/python3.11/site-packages
          key: ${{ runner.os }}-python-3.11-packages-${{ hashFiles('**/requirements.txt') }}-v1
          restore-keys: |
            ${{ runner.os }}-python-3.11-packages-

      # Cache SentenceTransformer model weights
      - name: Cache sentence-transformers models
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/torch
            ~/.cache/huggingface
          key: ${{ runner.os }}-sentence-transformers-all-MiniLM-L6-v2-v1
          restore-keys: |
            ${{ runner.os }}-sentence-transformers-

      # Install dependencies (feedparser added)
      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir sentence-transformers scikit-learn numpy torch feedparser

      # Preload and confirm SentenceTransformer model cache
      - name: Pre-download and cache model
        run: |
          python -c "from sentence_transformers import SentenceTransformer; model = SentenceTransformer('all-MiniLM-L6-v2'); print('✅ Model cached successfully')"

      # Run the daily feed curation script
      - name: Run Daily Curation
        run: python final_feed.py

      # Commit and push only if final.xml or last_seen_final.json changed
      - name: Commit and Push Final Feed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Ensure output files exist
          touch final.xml last_seen_final.json
          
          git add final.xml last_seen_final.json
          
          # Commit only when there are new changes
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📰 Daily curation: $(date -u +"%Y-%m-%d")"
            git push
          fi
